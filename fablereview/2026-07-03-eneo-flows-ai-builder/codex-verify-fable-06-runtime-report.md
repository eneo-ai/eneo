Peer loop skipped under your explicit hard constraint: `[no-peer-review]`.

**TL;DR**
1. Fable’s P0 is verified as a production blocker: stale-RUNNING reconciliation opens a production `autobegin=False` session, does not enable autobegin, does not enter `session.begin()`, and never commits.
2. The exact “first query raises `InvalidRequestError`” mode is strongly supported by local source/tests, but I did not empirically execute it because the prompt restricted verification to source inspection.
3. The idempotency/claim layer is mostly verified strong: run CAS, step CAS, attempt upsert idempotency, run/rerun fingerprints, and webhook/audit outbox claim mechanics are real.
4. The queue starvation and webhook-health findings are real; queue severity depends on deployed worker count/pool, while webhook health is a clear observability gap.
5. Best next slice: fix F-1 with one real-session integration test, then add webhook health signals and split maintenance task routing. Avoid broad rewrites.

**Verdict Matrix**

| Finding | Codex Verdict | Source Evidence | Confidence |
|---|---|---|---|
| F-1 stale-RUNNING reconciler has no transaction/commit | verified, with exact exception mode not directly executed | `async_sessionmaker(..., autobegin=False)` in [database.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/database/database.py:50); session context closes without commit in [database.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/database/database.py:95); reconciler lacks `enable_autobegin...`, `session.begin()`, and commit in [tasks.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/tasks.py:656); sibling tasks do enable/begin in [tasks.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/tasks.py:699); only unit mocks cover it in [test_celery_runtime.py](/Users/ccimen/eneo/eneo-flows-clean/backend/tests/unittests/flows/test_celery_runtime.py:934) | high |
| F-2 one Celery queue for execute + maintenance tasks | partially verified | all six task routes use `settings.flow_celery_queue` in [celery_app.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/celery_app.py:42); default queue/timeout are `flows.execute` and `3600` in [config.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/main/config.py:300); prefetch 1 in [app.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/worker/celery/app.py:24); deployment shows one `celery-worker-flows` service in [docker-compose.yml](/Users/ccimen/eneo/eneo-flows-clean/docs/deployment/docker-compose.yml:134). Actual severity depends on concurrency/pool and deployment scaling. | medium-high |
| F-3 health endpoint lacks webhook outbox signals | verified | health imports audit outbox but not webhook deliveries in [flow_runtime_health.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/flow_runtime_health.py:11); flags include audit only in [flow_runtime_health.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/flow_runtime_health.py:48); response has `audit_outbox` but no webhook section in [flow_runtime_health.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/flow_runtime_health.py:143); webhook dead-letter terminalizes run as failed in [flow_webhook_delivery.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/flow_webhook_delivery.py:388) | high |
| F-4 beat wrappers orphan coroutine on timeout | verified | `flows.execute` cancels and drains on timeout in [tasks.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/tasks.py:604); beat wrappers call `future.result(timeout=...)` with no cancellation in [tasks.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/tasks.py:813) | high |
| F-5 per-tenant reconciliation scales O(tenants) | partially verified | stale-running, review-expiry, and stale-queued all load tenants and loop in [tasks.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/tasks.py:670), [tasks.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/tasks.py:703), [tasks.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/tasks.py:742). Severity depends on tenant count and DB load. | medium |
| F-6 mid-flow webhook runtime guard missing | partially verified | validator enforces `http_post` only on terminal step in [flow_validators.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/flow_validators.py:214); executor returns RUNNING after pending webhook delivery intents in [executor.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/executor.py:1046); outcome returns `run_in_progress` while active results remain in [run_outcome.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/run_outcome.py:47). This is defensive against corrupted/published-invalid definitions, not a normal-path bug. | medium-high |
| F-7 implicit terminalizer/checkpoint and webhook exclusions | partially verified | FAILED terminalization only claims from active statuses in [flow_run_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_repo.py:434); AWAITING_REVIEW is not active in [enums.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/enums.py:152); checkpoint cancellation only on CANCELLED in [flow_run_terminalization.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/application/flow_run_terminalization.py:232); stale-running excludes pending webhook deliveries in [flow_run_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_repo.py:370) | medium-high |
| F-8 `flow_step_results` dual writer and false docstring | verified | docstring says `Writer: FlowRunRepository` in [flow_tables.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/database/tables/flow_tables.py:850); `FlowRunRepository` pre-seeds/claims/closes in [flow_run_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_repo.py:183), [flow_run_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_repo.py:840), [flow_run_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_repo.py:472); `FlowRepository.save_step_result` upserts results in [flow_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_repo.py:634); executor and webhook call it in [executor.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/executor.py:1702), [flow_webhook_delivery.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/flow_webhook_delivery.py:323) | high |
| F-9 rerun operation terminal status via string enum cast | verified | `FlowRunRerunOperationStatus(target_status.value)` inside terminal close path in [flow_run_rerun_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_rerun_repo.py:425) | high |
| F-10 deletion candidates | partially verified | `DISPATCH_FAILURE` appears only as enum plus unrelated test names in [enums.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/enums.py:265); `RETRIED` has no runtime writer by explicit guard test in [test_flow_run_status_capabilities.py](/Users/ccimen/eneo/eneo-flows-clean/backend/tests/unittests/flows/test_flow_run_status_capabilities.py:116) but remains in DB check constraint [flow_tables.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/database/tables/flow_tables.py:1149); `self.execution_backend` is assigned in [flow_run_service.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/application/flow_run_service.py:187) and not read; executor non-config path exists in [executor.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/executor.py:494) but tests use it. | medium-high |
| F-11 `updated_at` comment is misleading | verified | comment calls `updated_at` display metadata in [flow_tables.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/database/tables/flow_tables.py:731); stale queued/running use `updated_at` as fencing clock in [flow_run_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_repo.py:346), [flow_run_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_repo.py:399), [flow_run_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_repo.py:445) | high |
| F-12 undocumented outbox mechanics decision rule | partially verified | audit uses skip-locked rows/savepoints in [flow_run_audit_outbox_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_audit_outbox_repo.py:150) and [flow_run_audit_outbox_delivery.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/application/flow_run_audit_outbox_delivery.py:73); webhook uses token/TTL lease and HTTP outside transaction in [flow_run_webhook_delivery_repo.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/infrastructure/flow_run_webhook_delivery_repo.py:76) and [flow_webhook_delivery.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/flow_webhook_delivery.py:136). I found no decision-rule doc, but the proposed doc benefit is speculative. | medium |

**False Positives / Overclaims**

- F-1’s “raises on first query” was not directly verified by execution. Source strongly supports it: production sessions use `autobegin=False`, sibling tests state this raises, and other flow tasks explicitly enable autobegin. But under your constraints, exact runtime exception signature remains unexecuted.
- F-2 is a verified topology risk, not a fully proven outage. Starvation severity depends on worker concurrency, pool type, number of worker services, and real run duration distribution.
- F-10 overstates “two-minute delete” for the executor constructor path. Production passes `config` from [tasks.py](/Users/ccimen/eneo/eneo-flows-clean/backend/src/eneo/flows/runtime/tasks.py:331), but there are seven test call sites using `FlowRunExecutor(...)`; cleanup is still small, not zero-touch.
- Fable says webhook claim-lost rollback is tested. The transaction shape is correct, but the cited success-claim-lost test writes the same `{"text": "done"}` output before delivery and asserts it remains unchanged in [test_flow_webhook_outbox_delivery.py](/Users/ccimen/eneo/eneo-flows-clean/backend/tests/integration/flows/test_flow_webhook_outbox_delivery.py:741). That does not strongly prove rollback of a changed step result.
- F-12 is not implementation-ready as phrased. “Add one paragraph” is harmless, but this should not become a generic outbox framework or a broad docs project.

**Confirmed Production Blockers**

- **P0: stale-RUNNING recovery is broken or at least non-persistent.** The reconciler is the only caller of `terminalize_stale_running_run` from runtime task code, and it has neither autobegin enablement nor a transaction/commit. This blocks worker-crash recovery for runs that are already RUNNING. The missing test violates the repo testing standard’s preference for worker/runtime tests around crash recovery and terminalization in [testing-standard.md](/Users/ccimen/eneo/eneo-flows-clean/docs/engineering/testing-standard.md:5).

**Confirmed High-ROI Non-Blockers**

- **Webhook health signals:** add pending/backlog/dead-letter/expired-claim visibility to the existing health owner. This is small and directly operator-facing.
- **Maintenance queue split:** route beats/outboxes away from hour-long execution tasks. Keep it routing/deployment only, no new abstraction.
- **Beat timeout cancellation:** reuse the existing execute cancellation pattern for beat wrappers.
- **Step-result writer ownership:** move `save_step_result` into `FlowRunRepository` and fix the table docstring. This aligns with the canonical ownership rule in [maintainability-standards.md](/Users/ccimen/eneo/eneo-flows-clean/docs/engineering/maintainability-standards.md:29).
- **Dead-symbol cleanup:** delete `DISPATCH_FAILURE` and `FlowRunService.execution_backend`; treat `RETRIED` as a migration-backed cleanup; convert executor test helpers to `FlowRunExecutorConfig` before removing legacy kwargs.

**Implementation Backlog**

| Priority | Owner / Canonical Home | Smallest Safe Change | Acceptance Criteria | Tests |
|---|---|---|---|---|
| P0 | `backend/src/eneo/flows/runtime/tasks.py` | In `_reconcile_stale_running_runs_all_tenants`, enable autobegin and wrap tenant load plus each terminalization in explicit `session.begin()` transactions. | Old RUNNING run becomes FAILED with `flow_worker_stalled` and persists in a fresh session; step results/attempts close; audit outbox row exists. | One real-session integration test for the task, not a mocked session test. |
| P1 | `flow_runtime_health.py` | Add webhook outbox summary fields and flags mirroring audit: backlog, dead letters, expired claims. | Dead-lettered webhook row makes `/api/healthz/flows` UNHEALTHY; pending overdue row is DEGRADED. | Integration health test seeding `FlowRunWebhookDeliveries`. |
| P1 | `celery_app.py` + deployment config | Route maintenance tasks to `flows.maintenance`; keep `flows.execute` isolated. | `task_routes` test proves five maintenance tasks use maintenance queue; deployment has a consumer for it. | Unit route test plus compose/config validation. |
| P2 | `tasks.py` | Cancel and drain beat futures on timeout using the existing execute cancellation shape. | Slow beat coroutine is cancelled when wrapper times out. | Focused unit test with patched slow coroutine. |
| P2 | `FlowRunRepository` | Move `save_step_result` and file-row replacement out of `FlowRepository`; update callers. | `rg "save_step_result" flow_repo.py` is empty; runtime/webhook behavior unchanged. | Existing result/file mapping tests plus one late-write fencing test. |
| P2 | enums/migration/runtime tests | Delete `DISPATCH_FAILURE`; delete `execution_backend`; decide if `RETRIED` can be removed with DB check migration. | No references remain except migration history; pyright/ruff green. | Status capability tests updated; migration test if constraint changes. |
| P3 | `flow_run_rerun_repo.py` | Replace string enum cast with explicit terminal status map. | Adding a terminal run status fails an exhaustiveness test instead of terminalization runtime. | Small unit test over terminal statuses. |
| P3 | `flow_tables.py` | Reword `revision` comment to name `updated_at` as stale/recovery fencing metadata. | Comment no longer says display-only. | No runtime test needed. |
| Later | recovery repositories | Consider cross-tenant stale queries only after P0 is tested. | One query per beat tick without weakening tenant correctness. | Repository integration tests. |

**What Not To Fix Now**

- Do not build a generic outbox framework. Audit and webhook mechanics differ for a good reason: DB-only effect versus external HTTP side effect.
- Do not collapse `FlowExecutionBackend` just because it has one implementation. It is a small cross-process dispatch seam and currently earns its existence.
- Do not parallelize webhook delivery yet. Current serialized delivery is a throughput ceiling, but it is bounded and not the blocker.
- Do not do F-5 cross-tenant query rewrites before the F-1 real-session regression test exists.
- Do not add a runtime guard for every validator invariant immediately. F-6 is useful hardening, but normal published-flow paths already reject mid-flow `http_post`.

**Questions For Tomorrow**

- Can the P0 test be run first to capture the exact failure signature: first-query `InvalidRequestError` versus silent rollback/no commit?
- What is production Celery pool type and concurrency for `celery-worker-flows`? Source shows one service, but not runtime scale.
- Should `flow_task_timeout_seconds` ever be configured lower than the webhook retry horizon? If yes, health stale-running should probably exclude pending webhook deliveries like the reconciler does.
- Is `RETRIED` present in any non-disposable environment data, or can the DB check constraint be tightened before production?
- Should executor test builders be converted to `FlowRunExecutorConfig` in one mechanical change before deleting legacy kwargs?

**Verification Commands / Files Read**

- Read required inputs: `fable-06-operational-runtime-reliability-maintainability-review.md`, its prompt, `index.md`, `docs/engineering/maintainability-standards.md`, `docs/engineering/testing-standard.md`, and the Ponytail skill.
- Source inspection commands used: `sed -n`, `nl -ba`, `wc -l`, `rg -n`, `rg --files`, and `head`.
- Key searches: `terminalize_stale_running_run`, `_reconcile_stale_running_runs_all_tenants`, `enable_autobegin_for_flow_task_session`, `task_routes`, `worker_prefetch_multiplier`, `FlowRunWebhookDeliveries`, `save_step_result`, `DISPATCH_FAILURE`, `RETRIED`, `execution_backend`, `FlowRunExecutor(`.
- No source, tests, migrations, package files, config, or docs were edited.
- No internet, Claude, Fable, Antigravity, subagents, peer-loop tools, or test suites were run.
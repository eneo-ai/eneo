/** Input styling shared by the widget and template editors (matches the assistant editor). */
export const inputClass =
  "border-default bg-primary ring-default rounded-lg border px-3 py-2 shadow focus-within:ring-2 hover:ring-2 focus-visible:ring-2 w-full";

export const textareaClass = `${inputClass} min-h-24 placeholder:text-muted`;

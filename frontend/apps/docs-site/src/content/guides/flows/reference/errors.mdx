# Flow error reference

This page is for teams turning Eneo Flows error codes into clear recovery paths, and it shows which failures belong to request handling and which belong to run execution.

Use this as the canonical consumer reference for `FlowApiErrorCode`. Task guides link here instead of repeating code lists.

## How to handle codes

- Handle `Request path` codes from the failed API response before you create, edit, resume, or rerun a run.
- Handle `Run execution` codes from `run.error.code` while polling or rendering a terminal failed run.
- Handle `Request path or run execution` codes in both places; the same logical failure can surface synchronously or after worker execution.
- Unknown codes should degrade gracefully: show a generic Flow failure message, keep the raw code for support, and avoid automatic destructive retries.
- Do not automatically retry an LLM request timeout or ambiguous step execution failure: provider work may or may not have started, and a rerun can duplicate provider work and spend.
- A failed `per_source` or `per_item` step has no partial continuation: rerunning the step repeats every source or item provider call, including calls that may already have completed.

## Catalog

| Category               | Code                                                     | Handling phase                | Consumer action                                                                                                                                  |
| ---------------------- | -------------------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Flow access            | `flow_deleted`                                           | Run execution                 | Stop polling this run and require a restored or new flow before retrying.                                                                        |
| Flow access            | `flow_not_published`                                     | Request path                  | Publish the flow or refetch the runtime contract before retrying.                                                                                |
| Flow access            | `flow_owner_required`                                    | Request path                  | Retry with a principal that can own or administer the draft.                                                                                     |
| Flow access            | `flow_run_access_denied`                                 | Request path                  | Check tenant, space, service-key scope, and run ownership before retrying.                                                                       |
| Flow access            | `flow_service_key_admin_required`                        | Request path                  | Use an admin service key or call the published runtime endpoint.                                                                                 |
| Flow access            | `flow_service_key_principal_not_supported`               | Request path                  | Call with a user session or choose a service-key-supported runtime endpoint.                                                                     |
| Run input              | `flow_input_invalid_date`                                | Request path                  | Send the date in YYYY-MM-DD format.                                                                                                              |
| Run input              | `flow_input_invalid_multiselect_type`                    | Request path                  | Send an array of strings or a comma-separated string.                                                                                            |
| Run input              | `flow_input_invalid_multiselect_value`                   | Request path                  | Send every multiselect value as a string.                                                                                                        |
| Run input              | `flow_input_invalid_number`                              | Request path                  | Send a finite integer or decimal value for the number field.                                                                                     |
| Run input              | `flow_input_invalid_option`                              | Request path                  | Send only options listed in the published form schema.                                                                                           |
| Run input              | `flow_input_required_field_empty`                        | Request path                  | Send a non-empty value that matches the field contract.                                                                                          |
| Run input              | `flow_input_required_field_missing`                      | Request path                  | Read the form schema and send every required field before retrying.                                                                              |
| Run input              | `flow_input_type_mismatch`                               | Request path                  | Send the field value using the type declared in the form schema.                                                                                 |
| Run input              | `flow_run_aggregate_max_files_exceeded`                  | Request path                  | Reduce the total attached files before creating or rerunning the run.                                                                            |
| Run input              | `flow_run_concurrency_limit_reached`                     | Request path                  | Back off and retry after another run reaches a terminal status.                                                                                  |
| Run input              | `flow_run_file_not_accessible`                           | Request path                  | Upload the file through the same caller context or choose an accessible file.                                                                    |
| Run input              | `flow_run_file_not_bound_to_flow`                        | Request path                  | Upload files through the target flow before binding them to a run.                                                                               |
| Run input              | `flow_run_idempotency_conflict`                          | Request path                  | Retry with the original payload or choose a new idempotency key.                                                                                 |
| Run input              | `flow_run_input_payload_too_large`                       | Request path                  | Reduce text, JSON fields, or file-derived payload before retrying.                                                                               |
| Run input              | `flow_run_invalid_idempotency_key`                       | Request path                  | Send a stable idempotency key between 1 and 255 characters.                                                                                      |
| Run input              | `flow_run_invalid_step_inputs`                           | Request path                  | Validate step_inputs against the runtime contract before submitting.                                                                             |
| Run input              | `flow_run_required_step_input_missing`                   | Request path                  | Read the run contract and attach files to every required runtime-input step.                                                                     |
| Run input              | `flow_run_reserved_input_payload_key`                    | Request path                  | Rename caller-owned payload fields that collide with reserved keys.                                                                              |
| Run input              | `flow_run_runtime_input_disabled`                        | Request path                  | Refetch the run contract and only attach files to compatible steps.                                                                              |
| Run input              | `flow_run_stale_version`                                 | Request path                  | Refetch the runtime contract and submit against the current version.                                                                             |
| Run input              | `flow_run_step_input_file_too_large`                     | Request path                  | Check max file size in the run contract before upload or binding.                                                                                |
| Run input              | `flow_run_step_input_max_files_exceeded`                 | Request path                  | Limit attached files per step to the contract max_files value.                                                                                   |
| Run input              | `flow_run_step_input_mimetype_rejected`                  | Request path                  | Check accepted MIME types in the run contract before upload or binding.                                                                          |
| Run input              | `flow_run_top_level_file_ids_not_supported`              | Request path                  | Send files through step_inputs keyed by runtime step id.                                                                                         |
| Run input              | `flow_run_unknown_step_input`                            | Request path                  | Refetch the runtime contract and remove stale step ids.                                                                                          |
| Run input              | `flow_runtime_file_attached`                             | Request path                  | Do not delete files that are bound to run history.                                                                                               |
| Run input              | `flow_runtime_file_empty`                                | Request path                  | Reject empty files client-side before uploading when possible.                                                                                   |
| Run lifecycle          | `flow_dispatch_failed`                                   | Request path or run execution | Retry with backoff and alert an operator if dispatch keeps failing.                                                                              |
| Run lifecycle          | `flow_missing_principal`                                 | Run execution                 | Check service-key ownership and tenant principal state before retrying.                                                                          |
| Run lifecycle          | `flow_run_cancelled`                                     | Run execution                 | Stop polling and create a new run only if the user still needs output.                                                                           |
| Run lifecycle          | `flow_run_error_payload_invalid`                         | Run execution                 | Treat the run as failed and contact support with the run ID.                                                                                     |
| Run lifecycle          | `flow_run_redispatch_audit_unavailable`                  | Request path                  | Retry later with the same observed dispatch_exhausted_at value.                                                                                  |
| Run lifecycle          | `flow_run_redispatch_conflict`                           | Request path                  | Refresh the run and retry with its current dispatch_exhausted_at value.                                                                          |
| Run lifecycle          | `flow_run_user_cancelled`                                | Run execution                 | Treat the terminal state as intentional and do not auto-retry.                                                                                   |
| Run lifecycle          | `flow_runtime_actor_invalid`                             | Run execution                 | Refresh service-key or owner configuration before retrying.                                                                                      |
| Run lifecycle          | `flow_service_principal_disabled`                        | Run execution                 | Choose an enabled service principal or re-enable the configured one.                                                                             |
| Run lifecycle          | `flow_task_failure`                                      | Run execution                 | Inspect run logs and trace ids before retrying or escalating.                                                                                    |
| Run lifecycle          | `flow_task_timeout`                                      | Run execution                 | Retry with smaller input or inspect worker capacity and timeout settings.                                                                        |
| Run lifecycle          | `flow_worker_stalled`                                    | Run execution                 | Inspect worker and run health, then use the capability-gated rerun path or create a new run; escalate recurring stalls.                          |
| Evidence and artifacts | `flow_audit_outbox_delivery_not_found`                   | Request path                  | List dead letters again and verify the environment before retrying.                                                                              |
| Evidence and artifacts | `flow_audit_outbox_redrive_conflict`                     | Request path                  | List dead letters again and use the latest generation token.                                                                                     |
| Evidence and artifacts | `flow_evidence_audit_logging_failed`                     | Request path                  | Retry later and alert operators if audit logging remains unavailable.                                                                            |
| Evidence and artifacts | `flow_evidence_export_reason_required`                   | Request path                  | Collect and send a specific export reason with the request.                                                                                      |
| Evidence and artifacts | `flow_evidence_export_too_large`                         | Request path                  | Page this run's provider-call events through the provider-calls endpoint.                                                                        |
| Evidence and artifacts | `flow_run_artifact_content_unavailable`                  | Request path                  | Show retention-aware messaging and avoid retry loops for purged content.                                                                         |
| Evidence and artifacts | `flow_run_artifact_not_found`                            | Request path                  | Refresh run evidence and only request listed artifact ids.                                                                                       |
| Evidence and artifacts | `flow_run_evidence_forbidden`                            | Request path                  | Request evidence access with a principal allowed by run and policy checks.                                                                       |
| Evidence and artifacts | `flow_run_evidence_raw_export_forbidden`                 | Request path                  | Use the allowed evidence view or request a policy change.                                                                                        |
| Published definition   | `flow_assistant_snapshot_drift`                          | Run execution                 | Republish the flow so runtime dependencies match the stored snapshot.                                                                            |
| Published definition   | `flow_definition_checksum_mismatch`                      | Request path or run execution | Do not retry the pinned version; publish a valid version, start a new run, and retain the old run for evidence.                                  |
| Published definition   | `flow_definition_flow_id_invalid`                        | Request path or run execution | Treat the publication as corrupt, republish a valid version, then start a new run.                                                               |
| Published definition   | `flow_definition_invalid`                                | Run execution                 | Block new runs until an editor fixes and republishes the flow.                                                                                   |
| Published definition   | `flow_definition_no_executable_steps`                    | Run execution                 | Prevent run creation until at least one executable step is published.                                                                            |
| Published definition   | `flow_definition_schema_version_missing`                 | Request path or run execution | Require republish through the current editor, then start a new run against the new version.                                                      |
| Published definition   | `flow_definition_schema_version_unsupported`             | Request path or run execution | Republish with the current editor or upgrade runtime support, then start a new run.                                                              |
| Published definition   | `flow_definition_steps_invalid`                          | Request path or run execution | Block the pinned version, fix and republish the steps, then start a new run.                                                                     |
| Published definition   | `flow_input_contract_inapplicable`                       | Request path or run execution | Remove the conflicting contract or question binding, republish the flow, then start a new run.                                                   |
| Published definition   | `flow_published_form_schema_invalid`                     | Request path                  | Ask an editor to fix form fields and republish before running.                                                                                   |
| Step runtime           | `flow_provider_call_evidence_persistence_failed`         | Run execution                 | Inspect the provider-call page and the evidence gap in the run error before deciding whether to repeat a call whose remote outcome may be known. |
| Step runtime           | `flow_step_attempt_start_failed`                         | Run execution                 | Inspect run diagnostics and retry only after transient storage issues clear.                                                                     |
| Step runtime           | `flow_step_execution_failed`                             | Run execution                 | Do not auto-retry provider-calling failures: reruns can repeat provider work and spend; inspect step details first.                              |
| Step runtime           | `flow_step_missing`                                      | Run execution                 | Require republish and do not retry the stale run.                                                                                                |
| Step runtime           | `flow_webhook_delivery_failed`                           | Run execution                 | Read the result through run and step endpoints because delivery retries are exhausted and no public redelivery endpoint exists.                  |
| Typed input/output     | `flow_llm_request_timeout`                               | Run execution                 | Do not auto-retry: a rerun can duplicate provider work and spend; inspect the run before choosing smaller input or a faster model.               |
| Typed input/output     | `flow_mapped_provider_call_limit_exceeded`               | Run execution                 | Reduce mapped inputs or ask an administrator to raise the provider-call ceiling before rerun.                                                    |
| Typed input/output     | `flow_runtime_input_not_consumed`                        | Run execution                 | Update the authored step to consume step_input data or omit the files.                                                                           |
| Typed input/output     | `flow_unsupported_output_mode`                           | Run execution                 | Change the output mode and republish before starting another run.                                                                                |
| Typed input/output     | `flow_unsupported_output_type`                           | Run execution                 | Choose a supported output type and republish the flow.                                                                                           |
| Typed input/output     | `typed_io_audio_invalid_file_type`                       | Run execution                 | Validate accepted audio types before upload or rerun.                                                                                            |
| Typed input/output     | `typed_io_audio_missing_file`                            | Run execution                 | Attach the required audio file to the step before creating the run.                                                                              |
| Typed input/output     | `typed_io_audio_source_unsupported`                      | Run execution                 | Configure the step for runtime files or a supported previous step output.                                                                        |
| Typed input/output     | `typed_io_audio_too_many_files`                          | Run execution                 | Attach exactly the allowed number of audio files before starting the run.                                                                        |
| Typed input/output     | `typed_io_contract_violation`                            | Request path or run execution | Fix the JSON shape against the contract before saving or rerunning.                                                                              |
| Typed input/output     | `typed_io_document_source_unsupported`                   | Run execution                 | Configure the document step for a supported runtime input source.                                                                                |
| Typed input/output     | `typed_io_empty_extraction`                              | Run execution                 | Prompt the user for a readable file before rerunning.                                                                                            |
| Typed input/output     | `typed_io_file_not_found`                                | Run execution                 | Upload the file again and bind it through the current run contract.                                                                              |
| Typed input/output     | `typed_io_file_source_unsupported`                       | Run execution                 | Configure the file step for runtime files or a supported previous output.                                                                        |
| Typed input/output     | `typed_io_http_connection_error`                         | Run execution                 | Check target availability, DNS, and network allow rules before rerunning.                                                                        |
| Typed input/output     | `typed_io_http_invalid_config`                           | Run execution                 | Fix URL, method, headers, body, authentication, or response settings.                                                                            |
| Typed input/output     | `typed_io_http_invalid_url`                              | Run execution                 | Store a valid absolute URL and republish the flow.                                                                                               |
| Typed input/output     | `typed_io_http_malformed_response`                       | Run execution                 | Adjust the endpoint response or the step response parser settings.                                                                               |
| Typed input/output     | `typed_io_http_non_success`                              | Run execution                 | Handle the upstream status or change the target endpoint behavior.                                                                               |
| Typed input/output     | `typed_io_http_response_too_large`                       | Run execution                 | Request less data or expose a narrower endpoint for the flow.                                                                                    |
| Typed input/output     | `typed_io_http_ssrf_blocked`                             | Run execution                 | Use an allowed public endpoint or request an administrator policy review.                                                                        |
| Typed input/output     | `typed_io_http_timeout`                                  | Run execution                 | Reduce the request scope or increase upstream responsiveness before rerun.                                                                       |
| Typed input/output     | `typed_io_input_exceeds_model_window`                    | Run execution                 | Use a larger-context model, split the source document, or reduce the step input before rerun.                                                    |
| Typed input/output     | `typed_io_input_too_large`                               | Run execution                 | Reduce text, files, extracted content, or prompt context before rerun.                                                                           |
| Typed input/output     | `typed_io_invalid_file_type`                             | Run execution                 | Validate file type against the step contract before upload or rerun.                                                                             |
| Typed input/output     | `typed_io_invalid_input_source_combination`              | Run execution                 | Change the step input-source settings and republish.                                                                                             |
| Typed input/output     | `typed_io_invalid_input_source_position`                 | Run execution                 | Reorder or reconfigure the flow before republishing.                                                                                             |
| Typed input/output     | `typed_io_invalid_json_input`                            | Run execution                 | Validate the JSON value before rerunning the step.                                                                                               |
| Typed input/output     | `typed_io_invalid_output_mode_combination`               | Run execution                 | Change the output mode combination and republish the flow.                                                                                       |
| Typed input/output     | `typed_io_invalid_schema`                                | Run execution                 | Fix the JSON schema and republish before rerunning.                                                                                              |
| Typed input/output     | `typed_io_missing_required_files`                        | Run execution                 | Attach required files according to the step contract before rerun.                                                                               |
| Typed input/output     | `typed_io_output_parse_failed`                           | Run execution                 | Adjust prompt or schema, then rerun the failed step.                                                                                             |
| Typed input/output     | `typed_io_render_failed`                                 | Run execution                 | Inspect output content and document settings before rerunning.                                                                                   |
| Typed input/output     | `typed_io_template_checksum_mismatch`                    | Run execution                 | Republish the flow with the current template before rerunning.                                                                                   |
| Typed input/output     | `typed_io_template_render_failed`                        | Run execution                 | Check placeholders and referenced step outputs before rerunning.                                                                                 |
| Typed input/output     | `typed_io_transcript_too_large`                          | Run execution                 | Use shorter audio or split the task into smaller runs.                                                                                           |
| Typed input/output     | `typed_io_transcription_config_invalid`                  | Run execution                 | Fix transcription settings and republish before rerunning.                                                                                       |
| Typed input/output     | `typed_io_transcription_empty`                           | Run execution                 | Ask for audio with clear speech before rerunning.                                                                                                |
| Typed input/output     | `typed_io_transcription_failed`                          | Run execution                 | Retry after checking audio quality and provider availability.                                                                                    |
| Typed input/output     | `typed_io_transcription_model_missing`                   | Run execution                 | Choose an available transcription model and republish.                                                                                           |
| Typed input/output     | `typed_io_transcription_model_unavailable`               | Run execution                 | Choose another model or retry after provider recovery.                                                                                           |
| Typed input/output     | `typed_io_transcription_not_enabled`                     | Run execution                 | Enable transcription or change the step input type before republish.                                                                             |
| Typed input/output     | `typed_io_unsupported_type`                              | Run execution                 | Change the input type and republish the flow.                                                                                                    |
| Typed input/output     | `typed_io_validation_failed`                             | Run execution                 | Open step diagnostics and correct the input, schema, or configuration.                                                                           |
| Typed input/output     | `typed_io_variable_resolution_failed`                    | Run execution                 | Use the precise run error to correct the variable path, then republish or rerun.                                                                 |
| Review checkpoint      | `flow_review_already_resumed`                            | Request path                  | Treat duplicate resume as completed and reload the run status.                                                                                   |
| Review checkpoint      | `flow_review_cancelled`                                  | Request path                  | Reload the run and stop actions against the cancelled checkpoint.                                                                                |
| Review checkpoint      | `flow_review_checkpoint_not_found`                       | Request path                  | Refetch run review links and use a current checkpoint id.                                                                                        |
| Review checkpoint      | `flow_review_expired`                                    | Request path or run execution | Refetch the run and stop attempting review actions on the expired checkpoint.                                                                    |
| Review checkpoint      | `flow_review_idempotency_key_required`                   | Request path                  | Generate a stable retry key for each resume request.                                                                                             |
| Review checkpoint      | `flow_review_not_active`                                 | Request path                  | Reload the run and branch on the latest checkpoint state.                                                                                        |
| Review checkpoint      | `flow_review_not_approved`                               | Request path                  | Submit approve before resume, then retry resume with a retry key.                                                                                |
| Review checkpoint      | `flow_review_open_active_conflict_invariant`             | Run execution                 | Reload the run and escalate with the run ID if the conflict remains.                                                                             |
| Review checkpoint      | `flow_review_open_multiple_active_checkpoints_invariant` | Run execution                 | Reload the run and escalate with the run ID if multiple active rows remain.                                                                      |
| Review checkpoint      | `flow_review_open_step_result_incomplete_invariant`      | Run execution                 | Reload the run and escalate with the run ID if the state remains invalid.                                                                        |
| Review checkpoint      | `flow_review_policy_invalid`                             | Request path or run execution | Fix the review policy on the executable step, republish the flow, then start a new run.                                                          |
| Review checkpoint      | `flow_review_reject_reason_required`                     | Request path                  | Require a review rejection reason before submitting.                                                                                             |
| Review checkpoint      | `flow_review_reject_reason_too_long`                     | Request path                  | Truncate or ask the user to shorten the reject reason.                                                                                           |
| Review checkpoint      | `flow_review_rejected`                                   | Request path or run execution | Stop resume attempts and show the rejection reason if available.                                                                                 |
| Review checkpoint      | `flow_review_stale_revision`                             | Request path                  | Refetch the checkpoint and retry with the latest revision.                                                                                       |
| Review checkpoint      | `flow_review_step_result_not_found`                      | Request path                  | Reload evidence and avoid editing or approving a missing result.                                                                                 |
| Template asset         | `flow_template_corrupted_archive`                        | Request path                  | Ask the user to reopen and resave the file before upload.                                                                                        |
| Template asset         | `flow_template_in_use`                                   | Request path                  | Keep the template asset available while published versions can use it.                                                                           |
| Template asset         | `flow_template_invalid_archive`                          | Request path                  | Validate DOCX archives before upload when possible.                                                                                              |
| Template asset         | `flow_template_macro_not_allowed`                        | Request path                  | Accept only non-macro DOCX templates for Flow templates.                                                                                         |
| Template asset         | `flow_template_missing_content`                          | Request path                  | Ask for a new upload or restore the template file content.                                                                                       |
| Template asset         | `flow_template_missing_required_parts`                   | Request path                  | Reject the file and ask for a complete Word document template.                                                                                   |
| Template asset         | `flow_template_not_accessible`                           | Request path                  | Use a template stored in the flow or space with the right permissions.                                                                           |
| Template asset         | `flow_template_read_only`                                | Request path                  | Hide template replacement for read-only callers.                                                                                                 |
| Template asset         | `flow_template_unsupported_extension`                    | Request path                  | Allow only DOCX files for template upload.                                                                                                       |
| Rerun                  | `flow_run_rerun_attempt_lineage_conflict_invariant`      | Run execution                 | Reload the run and escalate with the run ID if lineage remains inconsistent.                                                                     |
| Rerun                  | `flow_run_rerun_invalid_transition`                      | Request path                  | Check status capabilities before showing or submitting rerun.                                                                                    |
| Rerun                  | `flow_run_rerun_multiple_active_operations_invariant`    | Run execution                 | Reload the run and escalate with the run ID if multiple active operations remain.                                                                |
| Rerun                  | `flow_run_rerun_reason_required`                         | Request path                  | Require a reason before submitting a rerun request.                                                                                              |
| Rerun                  | `flow_run_rerun_reason_too_long`                         | Request path                  | Truncate or ask the user to shorten the rerun reason.                                                                                            |
| Rerun                  | `flow_run_rerun_stale_revision`                          | Request path                  | Refetch the run and submit rerun against the latest revision.                                                                                    |
| Rerun                  | `flow_run_rerun_step_incomplete`                         | Request path                  | Offer rerun only for completed current step results.                                                                                             |
| Rerun                  | `flow_run_rerun_step_inputs_invalid`                     | Request path                  | Attach rerun files only to the step being rerun.                                                                                                 |
| Rerun                  | `flow_run_rerun_step_not_found`                          | Request path                  | Refetch run steps and only offer rerun for current step ids.                                                                                     |

Next: [Flows API Guide](/guides/flows-api-guide)

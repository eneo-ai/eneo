import { Callout } from "nextra/components";

# Flows API Guide

This guide is for developers building applications on top of published Eneo Flows, such as transcription services, municipality intake services, or other multi-step AI workflows.

It focuses on the **runtime** API. For the mental model behind drafts, steps, ownership, and evidence, start with <a href="/docs/flows">Flows</a>.

Reference owners: `FlowRuntimePathsPublic` defines runtime links, `FlowRunContractPublic` defines input/output fields, and `FlowApiErrorCode` defines machine-readable failures.

## Before you start

A few rules matter up front:

- only **published** flows can be run
- draft editing and AI Builder require a user principal
- a flow run is owned by the execution principal: a user principal or a service key
- service keys can run published flows, but they can access only **their own** runs and artifacts

### Authentication

Runtime examples in this guide use `Authorization: Bearer ...`. In practice that bearer token can be:

- a user token for a user principal
- an API key for a backend or integration

For the underlying auth model, see:

- <a href="/docs/api-key-management">API Key Management</a>
- <a href="/guides/authentication">Authentication & OIDC</a>

<Callout type="warning">
  Service keys do not get broad operator access. If a service key needs access
  to its own run evidence, it must have explicit <code>flow_evidence</code>{" "}
  capability on the key.
</Callout>

## Authentication and principal model

A Flow run is owned by the principal that created it.

- **Human user** → owns their own run
- **Service key** → owns the runs created with that key
- **Space owner / space admin** → trusted in-space operator for support/debug surfaces
- **Tenant admin** → trusted tenant-wide operator

### What “own run” means

An **own run** is a run created by the current principal. It does not mean “any run in my space”.

That distinction matters because service keys and ordinary human members are intentionally not cross-run operators.

## User-principal vs service-key runtime surfaces

### Endpoint families that require a user principal

| Endpoint family                           | Why it requires a user principal                                                        |
| ----------------------------------------- | --------------------------------------------------------------------------------------- |
| Draft CRUD (`POST/PATCH/DELETE /flows/…`) | Drafts are user-principal-owned authoring objects                                       |
| Publish / unpublish                       | Publishing is part of authoring governance                                              |
| Template management and inspection        | Template configuration belongs to draft authoring                                       |
| Flow-assistant authoring                  | Step-assistant configuration belongs to draft authoring                                 |
| AI Builder                                | Session-based user-principal drafting workflow                                          |
| `GET /api/v1/flows/{id}/`                 | Returns the current draft/current-definition view, not a service-key runtime projection |

### Service-key runtime endpoint families

| Endpoint family                           | Current service-key rule                                            |
| ----------------------------------------- | ------------------------------------------------------------------- |
| `GET /api/v1/flows/?space_id=...`         | Allowed for published-flow discovery in the key's scoped space      |
| `GET /api/v1/flows/{id}/published/`       | Allowed for a runtime-safe single-flow published projection         |
| `GET /run-contract/`                      | Allowed for published-flow runtime discovery                        |
| `GET /graph/`                             | Allowed for published/runtime-safe topology and own-run snapshots   |
| Runtime uploads and orphan upload cleanup | Allowed for published flows                                         |
| Run creation                              | Allowed for published flows                                         |
| List / get / cancel / redispatch runs     | Own runs only                                                       |
| Step outputs                              | Own runs only                                                       |
| Artifact downloads                        | Own runs only                                                       |
| Evidence                                  | Own runs only, and only when explicit evidence capability allows it |

![High-level Flows lifecycle diagram showing user-principal-only authoring, publishing to a versioned definition, and runtime execution with outputs, artifacts, and evidence](/diagrams/flows-overview.svg)

_Flows at a glance: user principals author and publish drafts, API consumers run published versions, and completed runs produce outputs, artifacts, and evidence._

## Common application patterns

For endpoint-and-capability-first design walkthroughs, start with [Designing Flows](/guides/flows/designing-flows). This reference section keeps the deeper API field context.

### Transcription service

A typical transcription app uses a first step with `transcribe_only` on uploaded audio, then adds later steps for summarization, redaction, classification, or document generation. This is usually a good fit for a **service key**, because the integration should not depend on one employee account.

### Municipality intake or case-work service

A municipality service often combines:

1. structured form fields and uploaded documents
2. retrieval over approved knowledge sources
3. generated text or DOCX output
4. evidence and highlighted source passages for explainability

This is where the run contract, runtime uploads, graph, artifacts, and evidence surfaces become especially valuable.

## Integration sequence

For a numbered consumer journey with errors to handle at each step, use [Integrating Flows](/guides/flows/integrating-flows). The sequence below is the compact reference checklist.

For most consumer apps, the safest order is:

1. list published flows in the scoped space
2. fetch the published runtime projection for the chosen flow
3. fetch the published run contract
4. upload files if the flow requires them
5. create a run with an optional `Idempotency-Key`
6. poll the run and step endpoints
7. if the run reaches `awaiting_review`, load the active review checkpoint
8. edit, approve, reject, or resume the checkpoint
9. optionally rerun a completed step when a reviewer accepts a correction
10. fetch artifacts or evidence if your principal is allowed to

## Tenant flow settings for runtime operators

Eneo also exposes **tenant-level Flow runtime settings** for operators and platform owners.

These are admin surfaces, not end-user runtime endpoints:

| Setting area                | Endpoint                                                                 | Purpose                                                                                                                  |
| --------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| Flow input limits           | `GET/PATCH /api/v1/settings/flow-input-limits`                           | Control maximum file/audio sizes and file-count limits                                                                   |
| Flow document render limits | `GET/PATCH /api/v1/settings/flow-document-render-limits`                 | Control generated PDF/DOCX rendering limits                                                                              |
| Flow runtime policy         | `GET/PATCH /api/v1/settings/flow-runtime-policy`                         | Control runtime step execution timeouts                                                                                  |
| Flow evidence policy        | `GET/PATCH /api/v1/settings/flow-evidence-policy`                        | Control sensitive-flow export defaults and stricter raw evidence export behavior, especially for classification-3 spaces |
| Flow retention policy       | `GET/PATCH /api/v1/settings/flow-retention-policy`                       | Configure tenant debug-evidence redaction for completed Flow runs                                                        |
| Classification retention    | `GET/PUT/DELETE /api/v1/settings/flow-classification-retention-policies` | Tighten full run-history and step-history purge windows for spaces assigned to a security classification                 |

AI Builder budget settings live at `GET/PATCH /api/v1/settings/ai-builder-budget`.
They are authoring/admin settings for AI Builder sessions, not Flow runtime controls.

### Flow retention policy shape

`/api/v1/settings/flow-retention-policy` exposes the implemented cleanup policy:

- `run_debug_evidence_days`

Send an integer day count to set the tenant debug-evidence override, or `null` in a PATCH request to remove that override.

This endpoint does **not** configure full run-history deletion. Flow and space `data_retention_days` settings are the default full run-history purge controls. Set a Flow override to `1..2555` days, or set it to `null` to inherit from the space. After a run is terminal and older than the effective window, Eneo deletes the Flow run, step results, step attempts, runtime file bindings, review checkpoints, webhook delivery history, audit outbox rows, and unreferenced generated result files. Delivered audit logs remain under audit retention so operators can still prove the Flow ran without retaining step content.

`/api/v1/settings/flow-classification-retention-policies` configures classification-aware full history purge:

- `GET /api/v1/settings/flow-classification-retention-policies` lists policy rows.
- `PUT /api/v1/settings/flow-classification-retention-policies/{security_classification_id}` creates or replaces a policy.
- `DELETE /api/v1/settings/flow-classification-retention-policies/{security_classification_id}` removes a policy. Delete is idempotent when the classification exists but has no policy row.

Each policy stores:

- `security_classification_id`
- `data_retention_days`

Classification policies tighten the Flow/space full run-history window. If a Flow has `data_retention_days = 30` and the classification policy is `2555`, the run is still eligible at 30 days. If a Flow and its space have no full-history retention window and the classification policy is `7`, terminal runs in spaces carrying that classification are eligible at 7 days.

Policies remain enforced even when tenant `security_enabled` display is disabled while spaces still retain `security_classification_id`. Deleting the security classification removes its policy and clears the space classification reference, which can loosen future dynamic retention back to the Flow/space policy. Use this deliberately when retiring a classification.

Purge is deferred while a run still has undelivered audit events or a queued/running rerun operation. Deferred runs are reported in cleanup-job counters so operators can resolve the blocker instead of silently losing audit or rerun state.

Current retention deletes the full run and step history. There is no API for raw per-step deletion intervals yet. For classification-3 or other sensitive workflows, use a stricter Flow/space `data_retention_days` window or a classification retention policy for full run-history purge, and use tenant evidence policy for export controls.

Reusable runtime uploads that are still owned outside a completed run are not deleted by the run-history purge. If a workflow needs upload-library cleanup independent of run history, that needs a separate file-lifecycle policy.

This is the intended operator-facing seam for sensitive care-data workflows. Avoid hardcoding these policies in consumer apps when an admin/operator setting is the more appropriate control point.

### Flow evidence policy shape

`/api/v1/settings/flow-evidence-policy` controls evidence export behavior for Flow runtime.

Key flags currently include:

- `allow_sensitive_flow_exports`
- `allow_space_admin_raw_export_class3`
- `allow_run_owner_raw_export_class3`
- `allow_service_key_raw_export_class3`

For sensitive care-data flows, the intended default is that evidence export stays disabled unless an operator intentionally enables it by policy.

### Sensitive-flow governance in `metadata_json`

For sensitive workflows, the flow definition may carry a `metadata_json.care_data_policy` object.

Current supported fields are:

- `sensitive`
- `approval_mode`
- `pre_approval_visibility`

Common values for that review model are:

- `approval_mode: "single_reviewer_outside_flow"`
- `pre_approval_visibility: "uploader_and_reviewers"`

This is the intended contract seam for workflows where review happens outside the flow but the workflow still needs explicit governance metadata.

Sensitive flows still deny evidence export by default until tenant flow settings explicitly enable `evidence_policy.allow_sensitive_flow_exports`, even for elevated human roles.

### 1) Discover what the published flow expects

#### Get the published runtime projection

```http
GET /api/v1/flows/{flow_id}/published/
```

Use this when you already know the flow id and want a **runtime-safe single-flow view** instead of the user-principal current-definition endpoint.

It is especially useful for service-key integrations because it gives you:

- stable published metadata such as name, description, and published version
- the canonical runtime paths to use next
- a clean alternative to calling `GET /api/v1/flows/{flow_id}/`, which still requires a user principal

Example response:

```json
{
  "id": "00000000-0000-0000-0000-000000000001",
  "space_id": "00000000-0000-0000-0000-000000000020",
  "name": "Employee Review Summary",
  "description": "Transcribe a review conversation and return a PDF summary.",
  "published_version": 3,
  "created_at": "2026-03-17T09:30:00Z",
  "updated_at": "2026-03-17T10:00:00Z",
  "runtime_paths": {
    "run_contract": "/api/v1/flows/00000000-0000-0000-0000-000000000001/run-contract/",
    "graph": "/api/v1/flows/00000000-0000-0000-0000-000000000001/graph/",
    "upload_step_runtime_file_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/steps/{step_id}/runtime-files/",
    "delete_runtime_file_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runtime-files/{file_id}/",
    "create_run": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/",
    "list_runs": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/",
    "cancel_run_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/cancel/",
    "rerun_step_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/steps/{step_id}/rerun/",
    "redispatch_run_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/redispatch/",
    "review_checkpoints": {
      "active_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/review-checkpoints/active/",
      "edit_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/review-checkpoints/{checkpoint_id}/",
      "approve_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/review-checkpoints/{checkpoint_id}/approve/",
      "reject_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/review-checkpoints/{checkpoint_id}/reject/",
      "resume_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/review-checkpoints/{checkpoint_id}/resume/"
    },
    "get_graph_for_run_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/graph/?run_id={run_id}",
    "get_run_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/",
    "list_steps_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/steps/",
    "evidence_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/evidence/",
    "export_evidence_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/evidence/export",
    "artifact_signed_url_template": "/api/v1/flows/00000000-0000-0000-0000-000000000001/runs/{run_id}/artifacts/{file_id}/signed-url/"
  }
}
```

<Callout type="info">
  Runtime paths are <strong>server-relative</strong> (for example{" "}
  <code>/api/v1/...</code>), so callers should prepend their API base URL when
  needed.
</Callout>

The full `runtime_paths` object includes `run_contract`, `graph`,
`upload_step_runtime_file_template`, `delete_runtime_file_template`,
`create_run`, `list_runs`, `cancel_run_template`, `rerun_step_template`,
`redispatch_run_template`, `review_checkpoints`,
`get_graph_for_run_template`, `get_run_template`, `list_steps_template`,
`evidence_template`, `export_evidence_template`, and
`artifact_signed_url_template`. The nested `review_checkpoints` object contains
`active_template`, `edit_template`, `approve_template`, `reject_template`, and
`resume_template`.

#### Get the run contract

```http
GET /api/v1/flows/{flow_id}/run-contract/
```

<Callout type="info">
  If you are integrating with a service key, the recommended discovery path is:
  list published flows in the scoped space, fetch{" "}
  <code>GET /api/v1/flows/{"{flow_id}"}/published/</code>, then call{" "}
  <code>run-contract</code>. Do not build service-key discovery around{" "}
  <code>GET /api/v1/flows/{"{flow_id}"}/</code>.
</Callout>

This endpoint is the canonical published runtime contract. It includes:

- `published_flow_version` for version mismatch detection
- `final_output` so clients know whether the terminal result is a payload or generated artifact
- `form_fields` for structured input fields
- `steps_requiring_input` for runtime step uploads
- `runtime_upload_policy` for upload timeout behavior
- `steps_requiring_review` for human-review UI preparation
- `aggregate_max_files`, which is the combined runtime-step file limit when the published flow can calculate one
- `template_readiness` for document-assembly readiness, such as whether a DOCX template is fully configured

Example:

```bash
curl -H "Authorization: Bearer $TOKEN" \
  https://your-domain.example/api/v1/flows/$FLOW_ID/run-contract/
```

Response shape:

```json
{
  "flow_id": "00000000-0000-0000-0000-000000000001",
  "published_flow_version": 3,
  "final_output": {
    "step_id": "00000000-0000-0000-0000-000000000104",
    "step_order": 3,
    "label": "Create Word report",
    "output_type": "docx",
    "output_mode": "pass_through",
    "delivery": "artifact",
    "output_contract": null
  },
  "form_fields": [
    {
      "name": "employee_name",
      "type": "text",
      "label": "Employee name",
      "required": true
    }
  ],
  "steps_requiring_input": [
    {
      "step_id": "00000000-0000-0000-0000-000000000101",
      "step_order": 1,
      "label": "Upload audio",
      "description": "Provide the recorded review conversation.",
      "required": true,
      "input_format": "audio",
      "max_files": 1,
      "max_file_size_bytes": 52428800,
      "accepted_mimetypes": ["audio/wav", "audio/mpeg"]
    }
  ],
  "runtime_upload_policy": {
    "min_timeout_seconds": 120,
    "seconds_per_mebibyte": 8,
    "max_timeout_seconds": 600,
    "idle_timeout_seconds": 120
  },
  "steps_requiring_review": [
    {
      "step_id": "00000000-0000-0000-0000-000000000103",
      "step_order": 2,
      "label": "Review transcription",
      "review_mode": "edit",
      "output_type": "json",
      "expires_after_seconds": 1209600,
      "output_contract": {
        "type": "object",
        "properties": {
          "transcription": { "type": "string" }
        }
      }
    }
  ],
  "aggregate_max_files": 1,
  "template_readiness": [
    {
      "step_id": "00000000-0000-0000-0000-000000000102",
      "template_asset_id": null,
      "template_file_id": null,
      "template_name": null,
      "checksum": null,
      "published_flow_version": 3,
      "status": "unavailable",
      "can_edit": false,
      "can_download": false,
      "message_code": null
    }
  ]
}
```

<Callout type="info">
  If your UI caches flow configuration, pass <code>published_flow_version</code>{" "}
  back into run creation so the server can reject stale submissions cleanly.
</Callout>

### 2) Upload files

```http
POST /api/v1/flows/{flow_id}/steps/{step_id}/runtime-files/
Content-Type: multipart/form-data
```

Use the step-specific runtime upload path from `runtime_paths.upload_step_runtime_file_template`.
Replace `{step_id}` with a step id from `steps_requiring_input` in the run contract.
Upload files before `create_run`; `runtime` means runtime input, not an already-started run mutation.

Example:

```bash
curl -X POST \
  -H "Authorization: Bearer $TOKEN" \
  -F "upload_file=@citizen-letter.pdf" \
  https://your-domain.example/api/v1/flows/$FLOW_ID/steps/$STEP_ID/runtime-files/
```

The returned file id belongs under `step_inputs[step_id].file_ids` when starting the run.
You may reuse the same file id under multiple compatible step ids when several steps should
process the same uploaded binary.

Upload response shape:

```json
{
  "id": "00000000-0000-0000-0000-000000000701",
  "name": "review-audio.mp3",
  "mimetype": "audio/mpeg",
  "size": 1843200,
  "created_at": "2026-03-17T10:04:00Z",
  "updated_at": "2026-03-17T10:04:00Z"
}
```

To abandon an upload before creating a run, call the cleanup path from
`runtime_paths.delete_runtime_file_template`:

```http
DELETE /api/v1/flows/{flow_id}/runtime-files/{file_id}/
```

Cleanup is only for orphan runtime uploads. After a file id is attached through
`step_inputs`, the run owns that mapping and deletion returns
`flow_runtime_file_attached` with HTTP 409.

### 3) Create the run

```http
POST /api/v1/flows/{flow_id}/runs/
Idempotency-Key: your-stable-client-key
```

Minimal request shape:

```json
{
  "expected_flow_version": 3,
  "input_payload_json": {
    "employee_name": "Alex Example"
  },
  "step_inputs": {
    "00000000-0000-0000-0000-000000000101": {
      "file_ids": ["00000000-0000-0000-0000-000000000701"]
    }
  }
}
```

Within each step input, `file_ids` are parsed as UUIDs and duplicate ids are
collapsed by first occurrence when the run is created. The remaining order is
preserved in the run input and contributes to SDK-derived idempotency keys. If a
prompt or document needs semantic ordering, model that ordering explicitly in the
flow rather than relying on file attachment order alone.

If the flow is still a draft, the API will return `flow_not_published`.

#### How `input_payload_json` maps to form fields

If the run contract exposes `form_fields`, send those values inside `input_payload_json` using the published field names as keys. The backend validates the values against the published field type and required/optional settings.

Reserved `input_payload_json` keys: `expected_flow_version`, `file_ids`, `step_inputs`, `transkribering`. These keys are owned by Flow run orchestration, so use different form field names and keep runtime file bindings in top-level `step_inputs`.

#### Why `Idempotency-Key` matters

Run creation supports caller-supplied idempotency. Reusing the same key with the same payload returns the existing run. Reusing the same key with a different payload returns:

- `400`
- code: `flow_run_idempotency_conflict`

This is especially useful for browser retries, webhook retries, and unreliable networks.

If you use `@intric/intric-js` (`frontend/packages/intric-js`), the Flow client accepts `idempotencyKey` on `flows.runs.create(...)` and exposes `flows.runs.deriveUploadIntentIdempotencyKey(...)` to build a stable key from the published flow id/version plus the uploaded runtime file bindings you submit in `step_inputs`. It also exports `FLOW_RUN_RESERVED_INPUT_PAYLOAD_KEYS` and `FlowRunReservedInputPayloadKey` for custom form validation. Use the same per-step file order for the key and the run creation request.

Example:

```js
const stepInputs = {
  [stepId]: { file_ids: [runtimeUpload.id] },
};

const idempotencyKey = await client.flows.runs.deriveUploadIntentIdempotencyKey(
  {
    flowId,
    expectedFlowVersion: runContract.published_flow_version,
    step_inputs: stepInputs,
  },
);

await client.flows.runs.create({
  flow: { id: flowId },
  expected_flow_version: runContract.published_flow_version,
  step_inputs: stepInputs,
  idempotencyKey,
});
```

### Audio-to-document runtime pattern

For the full speech-to-structured-report shape, see [Designing Flows](/guides/flows/designing-flows#speech-to-structured-report). The notes here keep the runtime reference constraints.

For an audio workflow that generates structured output or a DOCX artifact, the recommended runtime shape is:

1. record audio in the browser **or** select an audio file
2. upload the completed audio file through the published Flow runtime upload contract
3. derive a stable `Idempotency-Key` from the uploaded file ids and current run payload
4. create one published Flow run
5. let the first step transcribe and the later steps produce the final structured output / DOCX artifact

Important non-goals for this pattern:

- no live transcription streaming while the browser is still recording
- no frontend chunk orchestration for oversized files
- no separate browser-only retry protocol outside the shared `Idempotency-Key` contract

Approval and visibility boundary for sensitive audio-to-document flows:

- final approval is a **single authorized reviewer outside the flow**
- before final approval, generated output and run evidence should be treated as visible only to the uploader and explicitly authorized reviewers
- evidence export stays disabled by default unless policy explicitly enables it

This keeps the published Flow runtime aligned with the same enterprise seams used elsewhere: typed policy metadata, admin-configurable retention/export controls, and idempotent completed-upload execution.

### 4) Monitor and control the run

Key endpoints:

| Purpose                 | Endpoint                                                            |
| ----------------------- | ------------------------------------------------------------------- |
| Get status capabilities | `GET /api/v1/flows/runs/status-capabilities/`                       |
| List runs for a flow    | `GET /api/v1/flows/{flow_id}/runs/`                                 |
| Get one run             | `GET /api/v1/flows/{flow_id}/runs/{run_id}/`                        |
| Cancel a run            | `POST /api/v1/flows/{flow_id}/runs/{run_id}/cancel/`                |
| Redispatch a run        | `POST /api/v1/flows/{flow_id}/runs/{run_id}/redispatch/`            |
| Rerun a completed step  | `POST /api/v1/flows/{flow_id}/runs/{run_id}/steps/{step_id}/rerun/` |

Typical polling loop:

- create the run
- poll `GET /runs/{run_id}/`
- if the run reaches `awaiting_review`, load the active checkpoint and resolve it
- once the run becomes terminal, fetch step outputs, artifacts, and any evidence you need

The runtime API is asynchronous: create the run first, then poll for completion.

Use `GET /api/v1/flows/runs/status-capabilities/` when building run-history,
polling, cancellation, redispatch, and review UI logic. It returns canonical
booleans such as `should_poll`, `is_terminal`, `is_cancellable`,
`is_awaiting_review`, and `can_request_redispatch`, so your client does not need
to hard-code lifecycle groups.

Current run status values:

- `queued`
- `running`
- `awaiting_review`
- `completed`
- `failed`
- `cancelled`

`awaiting_review` is non-terminal. Keep polling and call the active checkpoint
endpoint before deciding whether to edit, approve, reject, or resume the run.

Example run response:

```json
{
  "id": "00000000-0000-0000-0000-000000000301",
  "flow_id": "00000000-0000-0000-0000-000000000001",
  "flow_version": 3,
  "principal_type": null,
  "tenant_id": "00000000-0000-0000-0000-000000000010",
  "trace_id": "00000000-0000-0000-0000-000000000302",
  "revision": 1,
  "status": "queued",
  "cancelled_at": null,
  "started_at": null,
  "finished_at": null,
  "input_payload_json": { "employee_name": "Alex Example" },
  "output_payload_json": null,
  "result_files": [],
  "token_usage": null,
  "error": null,
  "job_id": "00000000-0000-0000-0000-000000000401",
  "created_at": "2026-03-17T10:05:00Z",
  "updated_at": "2026-03-17T10:05:00Z"
}
```

#### What redispatch does

`redispatch` is for **stale queued runs**. It asks the system to try dispatching a queued run again if it appears stuck before execution started. It is not the same as creating a brand-new run.

If no stale queued work is found, the endpoint can still return `200` with `redispatched_count: 0`. Treat that as a no-op rather than as a new execution.

#### Human review checkpoints

The run contract's `steps_requiring_review` array tells you before run creation
whether the published flow can pause for review. When a run reaches
`awaiting_review`, use the path templates under `runtime_paths.review_checkpoints`.

```http
GET /api/v1/flows/{flow_id}/runs/{run_id}/review-checkpoints/active/
PATCH /api/v1/flows/{flow_id}/runs/{run_id}/review-checkpoints/{checkpoint_id}/
POST /api/v1/flows/{flow_id}/runs/{run_id}/review-checkpoints/{checkpoint_id}/approve/
POST /api/v1/flows/{flow_id}/runs/{run_id}/review-checkpoints/{checkpoint_id}/reject/
POST /api/v1/flows/{flow_id}/runs/{run_id}/review-checkpoints/{checkpoint_id}/resume/
```

Active checkpoint response shape:

```json
{
  "id": "00000000-0000-0000-0000-000000000901",
  "tenant_id": "00000000-0000-0000-0000-000000000010",
  "flow_id": "00000000-0000-0000-0000-000000000001",
  "flow_run_id": "00000000-0000-0000-0000-000000000301",
  "step_id": "00000000-0000-0000-0000-000000000101",
  "step_order": 1,
  "attempt_no": 1,
  "state": "awaiting_review",
  "revision": 1,
  "schema_version": 1,
  "original_payload_json": {
    "text": "Draft answer."
  },
  "current_payload_json": {
    "text": "Draft answer."
  },
  "step_label": "Review draft answer",
  "review_mode": "edit",
  "output_type": "json",
  "output_contract": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      }
    }
  },
  "next_step_ids": ["00000000-0000-0000-0000-000000000102"],
  "requester_user_id": "00000000-0000-0000-0000-000000000030",
  "requester_service_principal": null,
  "requester_principal_type": "user",
  "decided_by_user_id": null,
  "decided_by_service_principal": null,
  "decided_by_principal_type": null,
  "edited_at": null,
  "approved_at": null,
  "rejected_at": null,
  "resumed_at": null,
  "cancelled_at": null,
  "expires_at": "2026-03-31T10:05:30Z",
  "expired_at": null,
  "created_at": "2026-03-17T10:05:30Z",
  "updated_at": "2026-03-17T10:05:30Z"
}
```

Edit request body:

```json
{
  "expected_checkpoint_revision": 1,
  "current_payload_json": {
    "text": "Edited answer."
  }
}
```

Consumer sequence:

1. Poll the run until `status` is `awaiting_review`.
2. Call `active_template` and render `current_payload_json`.
3. Use `step_label`, `review_mode`, `output_type`, and `output_contract` to choose the review UI.
4. For edits, send `expected_checkpoint_revision` and the full corrected `current_payload_json`; this is not a JSON Patch document.
5. Approve or reject with the latest checkpoint `revision`.
6. Resume an approved checkpoint with the latest `revision` and a new `Idempotency-Key` header.

The revision is a compare token. If another reviewer changes the checkpoint
first, the API returns `flow_review_stale_revision`; reload the active
checkpoint and resubmit. Edits after `expires_at` return `flow_review_expired`.
Structured edit payloads are validated against the checkpoint `output_contract`
and can return `typed_io_contract_violation`.

Resume returns `202 Accepted`. Reusing the same resume idempotency key returns
the current checkpoint and run without dispatching duplicate work.

#### Rerun a completed step

Initial execution creates one run for the whole published flow. A rerun is
different: it re-executes one completed step in an existing completed or failed
run, invalidating that step and its downstream results.

Use `runtime_paths.rerun_step_template`:

```http
POST /api/v1/flows/{flow_id}/runs/{run_id}/steps/{step_id}/rerun/
```

Minimal request:

```json
{
  "expected_run_revision": 7,
  "reason": "The reviewer corrected the transcription for step 1."
}
```

`expected_run_revision` comes from the latest run response. `reason` is required
and stored with the rerun operation. You may also send `input_payload_json` and
`step_inputs`, but file overrides must be keyed by the rerun root step id. Upload
replacement files through the normal runtime upload path before requesting the
rerun.

Rerun returns `202 Accepted` with the rerun operation and current run. Redispatch
is only for stale queued runs that have not started; rerun is for deliberate
post-result correction.

### 5) Read results

#### Step outputs

```http
GET /api/v1/flows/{flow_id}/runs/{run_id}/steps/
```

This is the main endpoint for consumer UIs that want intermediate outputs, diagnostics, token counts, prompts, and structured payloads without depending on the heavier debug export.

Important runtime rule: initial execution creates one run for the whole published flow. If your UI wants per-step progress or completed-step results, create one run and then read the individual step states from this endpoint while the run is queued, running, awaiting review, completed, or failed.

Example step-list response:

```json
[
  {
    "id": "00000000-0000-0000-0000-000000000501",
    "flow_run_id": "00000000-0000-0000-0000-000000000301",
    "flow_id": "00000000-0000-0000-0000-000000000001",
    "tenant_id": "00000000-0000-0000-0000-000000000010",
    "step_id": "00000000-0000-0000-0000-000000000101",
    "step_order": 1,
    "assistant_id": "00000000-0000-0000-0000-000000000201",
    "status": "completed",
    "input_payload_json": {
      "diagnostics": [
        {
          "code": "runtime_input_consumed",
          "message": "Uploaded audio file was used.",
          "severity": "info"
        }
      ]
    },
    "runtime_input_file_ids": ["00000000-0000-0000-0000-000000000701"],
    "output_payload_json": {
      "text": "Hello and welcome to the annual review..."
    },
    "result_files": [],
    "current_attempt_no": 1,
    "num_tokens_input": 0,
    "num_tokens_output": 0,
    "error_code": null,
    "error_message": null,
    "diagnostics": [
      {
        "code": "runtime_input_consumed",
        "message": "Uploaded audio file was used.",
        "severity": "info"
      }
    ],
    "started_at": "2026-03-17T10:05:05Z",
    "finished_at": "2026-03-17T10:05:30Z",
    "created_at": "2026-03-17T10:05:05Z",
    "updated_at": "2026-03-17T10:05:30Z"
  }
]
```

Current step status values are `pending`, `running`, `completed`, `failed`, and `cancelled`.

#### Flow graph

```http
GET /api/v1/flows/{flow_id}/graph/
GET /api/v1/flows/{flow_id}/graph/?run_id={run_id}
```

Without `run_id`, the API returns the current live flow graph.

With `run_id`, it resolves the graph from that run's published version snapshot and annotates the nodes with run execution results. That is useful when you want to show the exact step topology and status history for a finished run.

#### Artifact downloads

```http
POST /api/v1/flows/{flow_id}/runs/{run_id}/artifacts/{file_id}/signed-url/
```

This returns a time-limited signed download URL for an artifact produced by that run. The client chooses the requested lifetime through the signed-URL request payload.

Request body:

```json
{
  "expires_in": 3600,
  "content_disposition": "attachment"
}
```

`expires_in` defaults to `3600` seconds when omitted. The current request model
does not enforce a minimum or maximum; choose short lifetimes that match your
product's security policy.

Typical response shape:

```json
{
  "url": "https://api.example.com/api/v1/files/00000000-0000-0000-0000-000000000701/download/?token=signed-token",
  "expires_at": 1773742500
}
```

### 6) Evidence and explainability

#### Evidence view

```http
GET /api/v1/flows/{flow_id}/runs/{run_id}/evidence/
```

Use this when you want the rich evidence payload for one run, including:

- the run summary
- the published definition snapshot
- step results
- step attempts
- the debug export payload

#### Evidence export

```http
GET /api/v1/flows/{flow_id}/runs/{run_id}/evidence/export?format=json&detail=redacted
GET /api/v1/flows/{flow_id}/runs/{run_id}/evidence/export?format=json&detail=raw
```

You can also pass a `reason` query parameter if you want the audit trail to capture why the export was performed.

For the full evidence and classification policy model, see <a href="/docs/flows">Flows</a>. The short version is:

- `evidence/` is the inline inspection surface
- redacted export is the normal support/compliance export
- raw export is the strictest export surface and may be tightened further in classification-3 spaces

#### Service keys and evidence

A service key can access evidence only for **its own runs**, and only if the key has explicit `resource_permissions.flow_evidence` capability. Those capability levels are conventional permission tiers for the evidence surface:

- `read` → evidence view
- `write` → redacted export
- `admin` → raw export, still subject to classification-3 tenant policy

#### RAG trace and highlighted segments

If a step used knowledge, the evidence/debug payload includes RAG provenance such as retrieved sources, chunk snippets, scores, and prompt-context inclusion metadata.

In the web UI, that same trace can open the source document and highlight the matched segments that influenced the answer.

## Reference

### Access matrix

Use this table as the canonical quick-reference for runtime access decisions.

| Endpoint family                                | Service key                                                 | Editor                              | Viewer                               | Space owner / admin         | Tenant admin              |
| ---------------------------------------------- | ----------------------------------------------------------- | ----------------------------------- | ------------------------------------ | --------------------------- | ------------------------- |
| Draft CRUD                                     | No                                                          | Per user-principal authoring policy | No                                   | Yes                         | Yes                       |
| Publish / unpublish                            | No                                                          | No                                  | No                                   | Yes                         | Yes                       |
| Template management / flow-assistant authoring | No                                                          | Per user-principal authoring policy | No                                   | Yes                         | Yes                       |
| AI Builder                                     | No                                                          | Per user-principal authoring policy | No                                   | Yes                         | Yes                       |
| List published flows in a space                | Yes, published only in scoped space                         | Yes                                 | Yes                                  | Yes                         | Yes                       |
| `GET /api/v1/flows/{id}/published/`            | Yes                                                         | Yes                                 | Yes                                  | Yes                         | Yes                       |
| `GET /api/v1/flows/{id}/`                      | No                                                          | Yes                                 | Read-only if visible by human policy | Yes                         | Yes                       |
| Run contract / runtime-safe graph              | Yes                                                         | Yes                                 | Yes                                  | Yes                         | Yes                       |
| Create run                                     | Yes                                                         | Yes                                 | No                                   | Yes                         | Yes                       |
| List / get / cancel / redispatch runs          | Own runs only                                               | Own runs only                       | No                                   | In-scope runs               | Tenant-wide runs          |
| Step outputs / artifacts                       | Own runs only                                               | Own runs only                       | No                                   | In-scope runs               | Tenant-wide runs          |
| Evidence view / export                         | Own runs only, explicit `flow_evidence` capability required | Own runs, policy-gated              | No                                   | In-scope runs, policy-gated | Tenant-wide, policy-gated |

### Principal visibility rules

The published runtime API is intentionally not “open to anyone who can see the flow”.

- user-principal run owners can inspect their own runs
- trusted in-space operators (space owner and space admin) can inspect in-scope runs, artifacts, and evidence
- tenant admins can inspect across the tenant
- service keys can inspect only their **own** runs and evidence
- user-principal run owners need `FLOWS_TRACE` for their own evidence access
- a service key never becomes a user-principal operator just because the key is scoped to a space with admin-level permission

### Standard error envelope

Flow endpoints use the platform's standard JSON error shape:

```json
{
  "message": "Flow must be published before creating runs.",
  "intric_error_code": 4000,
  "code": "flow_not_published",
  "context": null,
  "request_id": null,
  "details": null
}
```

The exact `intric_error_code` depends on the endpoint, but the envelope shape stays the same.

### Service-key authorization errors

Service-key clients can read published runtime projections and run published flows. Draft authoring, AI Builder, and user-principal actions remain user-token surfaces.

| Code                                       | When it appears                                                      | Client action                                                 |
| ------------------------------------------ | -------------------------------------------------------------------- | ------------------------------------------------------------- |
| `flow_service_key_admin_required`          | A non-admin service key reads `GET /api/v1/flows/{id}/`              | Use an admin/owner service key or call the published endpoint |
| `flow_service_key_principal_not_supported` | A service key calls a Flow endpoint that still requires a user token | Use a user token, or switch to a published runtime endpoint   |

`flow_service_key_admin_required` includes an API-adapter hint with an endpoint template:

```json
{
  "runtime_endpoint_hint": {
    "key": "published_flow_runtime",
    "description": "Use the published runtime projection for service-key Flow clients.",
    "endpoint_template": "/api/v1/flows/{id}/published/"
  }
}
```

Policy-layer denials such as `flow_service_key_principal_not_supported` may include only the runtime alternative key and description. Treat `endpoint_template` as optional and fall back to `GET /api/v1/flows/{id}/published/` when the hint is absent:

```json
{
  "runtime_endpoint_hint": {
    "key": "published_flow_runtime",
    "description": "Use the published runtime projection for service-key Flow clients."
  }
}
```

### Flow error catalog

The canonical consumer catalog for `FlowApiErrorCode` now lives in [Flow error reference](/guides/flows/reference/errors). Use that page for handling phase, recovery action, and exhaustive code coverage.

### Minimal end-to-end runtime example

A typical runtime-only integration looks like this:

```text
GET  /api/v1/flows/runs/status-capabilities/                 (cache status behavior)
GET  /api/v1/flows/{flow_id}/run-contract/
POST /api/v1/flows/{flow_id}/steps/{step_id}/runtime-files/ (optional)
DELETE /api/v1/flows/{flow_id}/runtime-files/{file_id}/      (optional abandoned upload cleanup)
POST /api/v1/flows/{flow_id}/runs/                           with Idempotency-Key
GET  /api/v1/flows/{flow_id}/runs/{run_id}/
GET  /api/v1/flows/{flow_id}/runs/{run_id}/review-checkpoints/active/ (when awaiting_review)
PATCH/POST review checkpoint edit/approve/reject/resume endpoints     (when awaiting_review)
GET  /api/v1/flows/{flow_id}/runs/{run_id}/steps/
POST /api/v1/flows/{flow_id}/runs/{run_id}/steps/{step_id}/rerun/      (optional correction)
POST /api/v1/flows/{flow_id}/runs/{run_id}/artifacts/{file_id}/signed-url/ (optional)
GET  /api/v1/flows/{flow_id}/runs/{run_id}/evidence/        (optional)
```

### Appendix: user-principal authoring and AI Builder surfaces

If your application also needs to create or maintain flows, there are two user-principal authoring surfaces.

#### Direct flow authoring

| Purpose                       | Endpoint                                       |
| ----------------------------- | ---------------------------------------------- |
| Create draft                  | `POST /api/v1/flows/`                          |
| List drafts / published flows | `GET /api/v1/flows/?space_id=...`              |
| Get draft                     | `GET /api/v1/flows/{flow_id}/`                 |
| Update draft                  | `PATCH /api/v1/flows/{flow_id}/`               |
| Publish                       | `POST /api/v1/flows/{flow_id}/publish/`        |
| Unpublish                     | `POST /api/v1/flows/{flow_id}/unpublish/`      |
| Delete draft                  | `DELETE /api/v1/flows/{flow_id}/`              |
| Upload DOCX template asset    | `POST /api/v1/flows/{flow_id}/template-files/` |

#### AI Builder

AI Builder lives under `/api/v1/flows/ai-builder/` and is a user-principal authoring surface. Service keys can run published flows, but they cannot draft or revise flows through AI Builder. For the product mental model and authoring details, see <a href="/docs/ai-builder">AI Builder</a>.

Plan responses expose a single `proposal` object. Use `proposal.spec` as the proposed draft Flow. For edit sessions, use `proposal.edit` for the approved base revision, explicit removed step refs, preview diff, warnings, advisories, risk flags, and confidence. `description_override_manual` records a reviewer decision to keep the current Flow description. Do not integrate against legacy `edit_result` or compiled-edit fields; those are not part of the public API contract.

For template-fill document output, treat the DOCX template asset path as an **accessibility-reviewed template path**. This is especially important for sensitive document workflows:

- upload reusable DOCX templates through the template asset route
- verify placeholders and mappings before publish
- prefer templates that have already been reviewed for heading structure, reading order, and table semantics

## Troubleshooting

For short capability answers and operational choices, see [Flows FAQ](/guides/flows/flows-faq). For Flow-specific codes, use [Flow error reference](/guides/flows/reference/errors) and branch on `code`, not message text.

| Status    | What it usually means                                                           | What to do                                                                                  |
| --------- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| `400`     | The request shape, state precondition, or Flow contract is invalid.             | Read the Flow error `code`, fix the request, and retry with idempotency.                    |
| `401/403` | Authentication, space scope, ownership, or evidence policy blocked the request. | Fix the principal or policy before retrying; platform auth may return `insufficient_scope`. |
| `409`     | The run, checkpoint, file, or idempotency key changed before this request.      | Reload the run or checkpoint and retry with the current revision.                           |
| `429`     | The tenant or flow has too many active runs.                                    | Back off and poll existing runs before creating more work.                                  |

## Practical takeaway

If you treat Flows as a **published contract plus a versioned runtime**, the API becomes straightforward:

- discover the contract
- upload what the contract requires
- create the run with idempotency
- poll run and step results
- fetch artifacts or evidence only within the visibility rules of the current principal

That is the cleanest way to build robust web applications on top of Eneo Flows.
